app = "minecraft-server-samuel"
primary_region = "sin"

# ❌ Remove or comment out this section:
# [build]
#   dockerfile = "Dockerfile"

[env]
  JAVA_TOOL_OPTIONS = "-Xmx1024M -Xms1024M"

[[services]]
  internal_port = 25565
  protocol = "tcp"
  auto_start_machines = true
  auto_stop_machines = false
  min_machines_running = 1

  [[services.ports]]
    port = 25565
